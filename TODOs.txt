UI:
    preview of where circle is gonna be
    size/position options in the image selection
    make box next to slider to input exact values
    sliders dont apply steps
    top left menu hover color change

other:
    add options:
        load path
        select saved path, give instructions for reproducing irl